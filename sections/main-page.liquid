<div class="wrap" style="padding-block:clamp(30px,4vw,56px)">
  <h1 class="h2">{{ page.title }}</h1>
  <div class="rte" style="max-width:76ch;margin-top:20px">{{ page.content }}</div>
</div>
{% schema %}{ "name": "Page" }{% endschema %}
